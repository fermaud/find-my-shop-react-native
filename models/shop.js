class Shop {
  constructor(id, title, imageUrl, city, coordinates) {
    this.id = id;
    this.title = title;
    this.imageUrl = imageUrl;
    this.city = city;
    this.coordinates = coordinates;
  }
}

export default Shop;
